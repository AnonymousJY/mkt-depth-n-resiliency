# ============================================================================
# Dependencies for mkt-depth-n-resiliency
# Code accompanying "Systematic Liquidity Risk Management: A Novel Perspective
# on Derivatives" (Yi & Kim).
#
# Target: Python 3.10+  (the project's compiled artifacts are cpython-310;
# the .idea Black config references 3.9 — 3.10 is the tested runtime).
#
# RECOMMENDED INSTALL PATH: environment.yml (conda). PyMC recommends
# conda-forge, which builds PyTensor's C / BLAS toolchain cleanly — this
# pip-only file is the fallback and builds PyMC/PyTensor from source, which
# needs a working C compiler.
#
# The version floors below are known-compatible minimums, not the exact
# versions used to produce the published results. For an exact-reproducible
# lock file, run the following inside the environment that generated the
# paper outputs:
#
#     pip freeze > requirements-lock.txt
#
# and commit requirements-lock.txt alongside this file.
# ============================================================================

# --- core numerical stack ---
numpy>=1.26,<3.0
scipy>=1.11
pandas>=2.0

# --- Bayesian estimation / MCMC (P-MLE calibration) ---
pymc>=5.16          # numpy 2.x support landed in 5.16
arviz>=0.18
pytensor>=2.25      # pulled in by pymc; pinned explicitly for clarity
nutpie>=0.13        # default NUTS sampler in RiskEngineKimYi2025.py

# --- market & reference data ---
FinanceDataReader>=0.9.50   # live SPX / COIN price series
databento>=0.34             # options data (used in the Study notebooks)
ustreasurycurve>=0.1        # US Treasury curve (discounting)
polars                      # required by ustreasurycurve; not auto-installed as a dependency
pyarrow>=14.0               # parquet snapshot I/O

# --- option-pricing utilities ---
nelson-siegel-svensson>=0.5  # NSS curve fit used in skew calibration

# --- plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- CLI ergonomics ---
tqdm>=4.65          # progress bars in skew_calibrate_{systematic,idiosyncratic}.py

# --- notebook execution ---
jupyter>=1.0
nbconvert>=7.0      # headless notebook runs for the reproducibility harness

# --- optional GPU acceleration -----------------------------------------------
# ImportLibs.py transparently swaps numpy -> cupy when a CUDA GPU is present.
# Install the wheel matching your CUDA toolkit, e.g.:
#   pip install cupy-cuda12x
# Leave uninstalled to run on CPU (NumPy).
# cupy-cuda12x>=13.0
